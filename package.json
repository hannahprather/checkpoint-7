{
  "name": "checkpoint-7",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:server && npm run setup:client",
    "setup:client": "cd checkpoint-7.client && npm i",
    "setup:server": "cd checkpoint-7 && npm i",
    "start": "node checkpoint-7/index.js"
  },
  "engines": {
    "node": "^14.17.0"
  }
}
